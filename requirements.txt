# 图像处理
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# OCR (macOS only) - 使用优化版本
ocrmac @ git+https://github.com/Yinwhe/ocrmac.git ; platform_system == "Darwin"
pyobjc-framework-Vision>=9.0 ; platform_system == "Darwin"

# 进度条
tqdm>=4.65.0

# 文档处理
PyMuPDF>=1.23.0

# Web 服务器（用于审查界面）
Flask>=3.0.0
Flask-CORS>=4.0.0
