# legacy_review_results

这里存放依赖旧 `review_results.json` / `segmentation_review.json` 数据结构的维护脚本。

当前人工审核真源是：

- `data/results/manual/review_books/*.json`
- `data/results/manual/segmented/`

这些 legacy 脚本只用于追溯历史迁移过程，不应直接用于当前数据。
